;Hacer una función que reciba un valor entero N y devuelva un vector de tamaño N,
;donde cada posición contiene el valor -1.
(display"Ingrese el tamaño del vector:(N)")
(newline)
(define funcion (display (make-vector (read) "-1")))
;llamado
funcion